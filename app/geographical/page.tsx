export default function GeographicalPage() {
  return (
    <main className="min-h-screen bg-paper px-8 py-12 md:px-16">
      <h2
        className="font-sans font-[400] leading-[0.95] text-ghost-faint"
        style={{ fontSize: "var(--text-display)" }}
      >
        Geographical
      </h2>
      <p
        className="mt-6 font-mono uppercase text-ghost-mid"
        style={{
          fontSize: "var(--text-label)",
          letterSpacing: "var(--tracking-nav)",
        }}
      >
        Coming soon
      </p>
    </main>
  );
}
